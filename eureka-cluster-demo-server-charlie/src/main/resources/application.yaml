server:
  port: 8763
spring:
  application:
    name: eureka-server-charlie
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      default-zone: http://eureka-server-alpha.com:8761/eureka, http://eureka-server-bravo.com:8762/eureka
  server:
    # 自我保护机制 关闭
    enable-self-preservation: false
  instance:
    hostname: eureka-server-charlie.com